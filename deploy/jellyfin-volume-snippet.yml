# Merge the /trailers entry into the existing Jellyfin service.
# This is a snippet, not a replacement for the complete Compose file.
services:
  jellyfin:
    volumes:
      - /path/to/jellyfin/trailer-reel/trailers:/trailers
